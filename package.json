{
  "name": "footlab-os",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first operating system for longitudinal at-risk foot monitoring and personalized insert manufacturing.",
  "scripts": {
    "check": "node scripts/check-foundation.mjs",
    "demo:capture": "node scripts/demo-capture-server.mjs",
    "demo:dataset": "node --experimental-strip-types scripts/demo-dataset.mjs",
    "test:dataset": "node --experimental-strip-types --test packages/dataset-engine/test/*.test.mjs"
  }
}
